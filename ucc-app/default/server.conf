# Replicate the MCP tool registration config across search head cluster members
# (AppInspect check_custom_conf_replication: custom .conf files need an explicit
# conf_replication_include entry to survive SHC replication).
[shclustering]
conf_replication_include.tools = true
